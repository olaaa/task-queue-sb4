package com.example.paymentqueue.model;

/**
 * Идентификатор захваченного платёжного поручения. НЕ маппится в БД!
 *
 * retryCount кэшируется при захвате и используется как "идентификатор воркера":
 * если retryCount в базе изменился — планировщик уже передал задачу другому воркеру,
 * текущий должен немедленно остановиться.
 */
public record PaymentTaskId(long id, short retryCount) {}
