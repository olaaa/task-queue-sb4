spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/paymentqueue
    username: olga   # твой macOS username
    password:        # пусто
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/paymentqueue
    username: olga
    password:

  flyway:
    enabled: true
    locations: classpath:db/migration
    url: jdbc:postgresql://localhost:5432/paymentqueue
    user: olga
    password:

payment-queue:
  stale-timeout-minutes: 5
  scheduler-interval-ms: 2000
  lock-retry-count: 3
#
#logging:
#  level:
#    org.flywaydb: DEBUG